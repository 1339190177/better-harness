# Better Harness 产品规划 2026

> 来源：`better-harness-产品规划-20260911.html`（正文更新至 2026-09-12）。
> 本文是该规划稿的结构化转写，不合并、不参照仓库现有 `roadmap.md`。
>
> **阅读边界（原文明确声明，转写时保留）**
>
> - Studio 界面依据本地构建的现状基线还原（1440 × 900 · 浅色 · 隔离空项目），只验证共用工作台外壳，未运行真实模型。
> - 插件形态以交互脚本与报告内容方案说明，不是 Codex 实机截图；本轮未安装或实跑插件。
> - 新增导航与业务内容均为**规划示意**，出现在文中不代表已实现。
> - 文中的耗时与消耗数字是个案线索，不构成产品性能基准。
> - 阶段表表示**能力建设顺序**，不承诺排期、团队容量或发布名单。

一句话主张：**按任务选好、配好，让每次调整都有依据。**

---

## 1. 背景与问题

Agent 已能完成越来越复杂的工作，但用户仍很难判断一次任务的高消耗、长耗时和反复失败是否合理。配置数量持续增加，规则、Skill、Agent、Hook 与工具之间的关系也更难看清。

- **开始使用时**：需要决定怎样使用 Agent、哪些配置与当前任务有关；默认能力可能够用，也可能需要局部调整。
- **日常使用时**：问题转向成本、等待与可靠性。
- **项目维度**：项目刚建立时，团队常不清楚哪些规则、工具和验证已经够用；项目演进后，旧约束的用途、维护责任和退役条件又容易失去记录。**项目所处阶段与 Harness 的成熟度并不总是一致。**

| 用户遇到的问题 | 当前判断困难 | 对结果的影响 |
| --- | --- | --- |
| 不知道怎么选配 | 产品、使用形态、模型与配置混在一起；推荐插件不等于已安装 | 多做设置，仍不清楚能否支持实际任务 |
| 花费高 | 总量无法区分必要工作、重复输入与失败重试 | 只换便宜模型可能损害任务质量 |
| 等待久 | 执行、审批、Hook、工具等待与并发工作混在一起 | 看见任务慢，却找不到可处理的区间 |
| 配置复杂 | 存在、加载、调用、影响决策和改善结果是不同事实 | 组件越来越多，问题仍可能反复出现 |
| 改过却不知有没有用 | 当前验证与后续任务表现缺少持续关联 | 优化停留在“命令成功”或“检查通过” |

**近期目标用户**：编码 Agent 的个人重度使用者、前线支持、Harness 开发者。
**目标**：让问题判断有依据、下一步可执行、改进能得到验证。

---

## 2. 用户场景

| 人物与情境 | 要完成的任务 | 期望结果 |
| --- | --- | --- |
| 使用者 · 只把 Codex 当任务执行 Runtime | 按用途盘点实际能力与设置，保留必要工具和工作约束 | 最小可用方案；可零修改，作用域、生效与恢复可核对 |
| 集成开发者 · 尚未确定运行方案 | 区分产品、CLI/桌面/IDE/SDK 形态、模型与能力组合 | 沿用当前、换形态或有限候选；先判断适配，再试用验证 |
| 重度使用者 · 安装或迁移配置后 | 核对 Rule、Skill、Agent、Hook 与工具的协作关系 | 找到真实冲突、断链和待核对关系 |
| 使用者 / 支持人员 · 某次消耗明显偏高 | 解释钱或额度花在了哪里 | 有证据的构成与可验证的改善方向 |
| 使用者 / 支持人员 · 任务很久不结束 | 区分执行与等待，定位主要区间 | 知道能处理什么、还缺什么信息 |
| 使用者 · 长任务膨胀、压缩后反复纠偏 | 理解上下文变化及其可见影响 | 保留事实与未知，避免武断归因 |
| 使用者 / Harness 开发者 · 已采用建议 | 核对修改正确性与后续效果 | 可回看、可复验、必要时可恢复 |
| 跨 Agent 用户 · 同项目有多份历史 | 整理各宿主的工作证据 | 看清差异及不可比条件 |
| 开发者 / 评测作者 · 要选模型或配置 | 在相同任务约束下比较方案 | 质量合格后再比较成本与时间 |
| 重度使用者 · 多个来源的记忆积累 | 理解过期、冲突、冗余与来源 | 有引用的分析，保持来源隔离 |
| 项目发起者 · 新项目尚无任务历史 | 判断首个任务需要哪些工作约束、工具与检查 | 够用、能验证、不过度配置的起点；最小准备纳入 A 后场景选配试点 |
| 项目维护者 · 存量规则积累或环境迁移 | 判断哪些保留、补齐、调整或退役 | 能说明每项调整的依据，保留依赖关系与恢复方式 |
| 交接双方 · 项目换人或进入归档 | 理解现有 Harness 的用途、来源、维护责任与已知问题 | 知识可接续、风险可追溯；不因归档自动删除配置或数据 |

---

## 3. 产品方案

**定位**：帮助用户按任务选好、配好并持续改进 Agent 的运行环境，让每项配置有用途、每次调整可验证。

主链条：

```text
理解用途 → 选定或沿用运行环境 → 配置与验证 → 日常体检诊断 → 改进复验 → 随场景变化持续治理
```

可以借鉴 360 的“盘点—处理—复查”体验，但 Agent 需要以任务目标判断适配：**未使用不等于无用，减少插件数不等于减少实际成本**。已有明确故障的用户直接进入诊断，无须先完成场景问答。

### 3.1 五个能力域

| 能力域 | 回答什么 | 承接功能 |
| --- | --- | --- |
| 场景选配 | 这次用途需要怎样的环境 | 场景与配置；采用后进入改进记录 |
| 体检与诊断 | 哪里有问题、依据是什么 | 健康概览、配置与关系、任务诊断 |
| 改进与复验 | 改什么，改后是否有效 | 建议、实施、生效验证、恢复与后续效果 |
| 比较与运行方案建议 | 哪些能用，哪些实测更好 | 有限能力筛选、历史比较、隔离实验、效果优选 |
| 资产与持续治理 | 项目变化后怎样接续 | Memory、配置用途与证据；长期治理分层推进 |

原则：结论优先，原始证据按需深入；质量约束贯穿成本和效率优化。**信息不足时保留缺口，不强制给出健康总分。**

### 3.2 项目生命周期视角

新项目从目标和第一项任务出发，形成最小可用的工作约束；存量项目先理解现有配置及使用证据，再决定怎样优化。两条路径汇入同一条诊断、实施、验证与后续观察的改进过程。

| 项目阶段 | 此时需要回答的问题 | Harness 应承担的责任 |
| --- | --- | --- |
| 新建与准备 | 第一项任务需要什么工作约束？ | 理解目标、复用已有能力、提出最小配置与验证方式；最小准备归入场景选配 |
| 首个任务 | 配置是否真正被加载、采用并支撑交付？ | 分别检查已配置、已加载、已采用、结果通过，留下第一份工作证据 |
| 日常迭代 | 为什么贵、慢、反复失败或偏离约束？ | 使用已有静态体检、任务诊断与改进复验；先处理具体问题 |
| 发布或关键变更 | 约定的检查是否满足，出错后能否恢复？ | 核对验证覆盖、关键限制与恢复准备；**不扩展成发布系统** |
| 演进与迁移 | 哪些约束要保留、调整、替换或退役？ | 结合项目需要、依赖和使用证据治理；长期版本与漂移追踪为新增提案 |
| 交接与归档 | 下一位维护者如何理解和接续？ | 保存用途、来源、维护责任、证据与恢复方法；**不自动删除或停止服务** |

**规划边界**：最小初始化已并入场景选配，放在 A 后单宿主试点；有限运行方案筛选随后按价值与可维护性推进。持久配置治理、自动切换、迁移交接继续评估。原一期与 B/C 保持。
**项目生命周期、Harness 成熟度、A/B/C 交付阶段是三个不同维度**：新项目可以继承成熟团队配置，老项目也可能刚开始使用 Agent。

### 3.3 双形态：同一能力，两种工作场景

| 形态 | 定位 |
| --- | --- |
| 宿主插件 / 内置入口 | 在当前项目中提出问题、看结论与证据，接着完成已授权的改进。以 Codex 为例，使用任务对话、详细报告和原生变更审阅；一次诊断与复验可以独立完成 |
| Studio / 独立应用 | 看清多个 Agent 与项目的差异。以统一工作台承载深度证据、历史观察、受控实验和多来源记忆分析 |

共用诊断口径与证据基础：静态关系检查 / 动态任务分析 / 结论解释 / 改进验证。

- 生命周期不决定使用哪种形态。插件也可承接 A 后试点的新项目最小准备，Studio 也可用于新项目。
- 选择取决于当前项目上下文，还是多来源、长时间跨度与专业分析。
- 两种形态共享业务含义，按各宿主实际能力开放功能。
- 开源属于发布与协作方式，插件和桌面都可以开源。
- 插件默认解决当前环境的问题；用户询问选型时给出有来源的中立建议，**无须强制转入 Studio**。

### 3.4 两类证据，保留五维解释框架

| 证据方式 | 覆盖内容 | 解释落点 |
| --- | --- | --- |
| 静态体检 | 规则、组件职责、引用关系、Hook 与工具配置、验证入口 | 任务理解 · 受控执行 · 变更验证 · 可靠交付 · 学习沉淀 |
| 动态诊断 | 任务消耗、耗时、调用、上下文、重试与产物证据 | 沿用同一五维 Work Loop；先围绕用户当前问题呈现 |

### 3.5 Studio 信息组织

**日常工作**

| 视图 | 内容路径 |
| --- | --- |
| 场景与配置 | 用途与约束 → 最小配置方案 → 采用与验证 |
| 健康概览 | 需要关注什么、范围是否完整 → 问题详情与证据 |
| 配置体检 | 组件与关系问题 → 配置证据和建议 |
| 任务诊断 | 消耗、耗时、上下文与异常 → 任务详情 |
| 改进记录 | 建议、实施、复验与后续效果 → 建议与验证详情 |

**专业工作**：比较实验（运行方案筛选、历史观察、受控实验及有条件效果推荐）、记忆（来源、阅读、选定快照分析）、高级工作区（保留既有 Debugger、Artifacts、Harness 能力）。
**设置**：环境与数据（宿主、项目、范围、分析去向与报告导出）。

规则：健康概览继续作为默认入口，场景选配按支持情况显示。Codex 插件通过对话和报告承接这些能力。问题、任务、建议详情由上下文进入；**返回时保留筛选、对象和阅读位置**。AI 解释与当前对象绑定，报告导出属于当前报告的动作。

### 3.6 沿用现有基础，补齐产品闭环

| 现有基础 | 规划中的增强 |
| --- | --- |
| 插件分析、Inspector 与 Studio 工作台 | 以用户问题组织入口，连接结论、证据与动作 |
| Qoder 首个细粒度性能分析实现 | 把统计变成可解释诊断，逐宿主标明支持深度 |
| 历史比较、Live Compare、隔离实验基础 | 明确各模式结论权限，补质量与可比性约束 |
| Memory 目录、阅读与显式分析 | 增强来源、冲突、过期、冗余与引用分析 |
| 桌面封装与打包入口 | 安装、签名分发、更新与原生平台质量另行验收 |

---

## 4. 方案流程

### 4.1 三条进入路径

| 进入路径 | 用户过程 | 本轮完成的落点 |
| --- | --- | --- |
| 尚未确定运行方案 · 有限筛选 | 说明硬约束 → 核对产品与形态 → 查看适配和缺口 → 沿用当前或准备试用 → 回到场景选配 | 有来源的有限候选与待试用条件；**无可比任务不宣布性能赢家** |
| 新项目 / 新用途 · A 后试点 | 说明目标与首个验收任务 → 复用现有能力 → 审阅最小配置建议 → 在授权范围内采用 → 核对加载与关键路径 → 完成首个代表性任务 | 记录采用范围、配置和第一份任务证据；**没有历史时不宣称节省或优化幅度** |
| 存量项目 | 盘点现有配置、来源与依赖 → 对照项目需要与使用证据 → 定位冲突、冗余或缺口 → 选择具体调整 → 验证并保留恢复方式 | 当前诊断与复验沿用既有能力；持久历史、复发与漂移治理作为扩展规划 |

### 4.2 共同主链路

```text
选择范围 → 发起体检或任务分析 → 看结论与证据 → 审阅并采用建议
        → 实施和当前验证 → 用后续可比任务检验效果
```

| 步骤 | 用户看到或执行 | 分支与结果 |
| --- | --- | --- |
| 选择范围 | 明确项目、宿主、任务或时间范围 | 能力部分可用时说明限制；**不默认扩大读取范围** |
| 理解诊断 | 先看优先问题，再深入配置段落、调用或时间区间 | 证据不足时补充材料；可标误判、忽略或维持现状 |
| 审阅建议 | 核对改动对象、内容、验证与恢复方式 | 当前状态变化时重新审阅；已有具体授权可继续执行 |
| 实施与复验 | 观察实施记录及匹配该修改的检查结果 | 失败保留原因；必要时恢复；**通过仅代表当前验证** |
| 追踪效果 | 在质量不退步的可比任务上看成本、耗时和异常 | 支持 / 无变化 / 冲突 / 不可比；不足时继续观察 |

三步示例（规划示例）：① 诊断结果「相似搜索反复出现」→ 先核对任务与引用证据；② 改进建议「补充明确的搜索停止条件」→ 审阅对象、差异与验证方式；③ 改进记录「当前验证通过 / 后续任务效果：待观察」→ 用新的可比任务核对实际效果。

---

## 5. 功能规划（9 项）

### 功能 1 · 场景与配置（A 后试点 · 单宿主、有限用途）

把“我要完成什么”变成一份适合当前宿主的最小配置方案。先复用已知目标，核对实际能力和有效配置；**已有默认足够时允许零修改**。准备过程不依赖历史任务。

五个步骤：

1. **盘点依据**：推荐目录、已安装、启用、任务可见、已调用和任务结果分别记录。没有调用历史时保留未知。
2. **最小方案**：保留任务所需工具与约束；不相关项只有查明已安装、依赖和影响后才提出调整。默认足够时可直接进入验证。
3. **采用前**：核对版本、有效值与来源、共享依赖、加载条件及前状态。项目范围可满足时不扩大到全局。
4. **准备结果（当前待验证）**：分别核对实际修改、宿主生效和代表性任务。后续效率没有对照证据时保持未知；失败可按记录恢复。
5. **动作**：保留当前配置 / 查看运行方案建议。

约束：

- 只补影响方案的未知；Runtime 产品、使用形态、模型/服务与配置分别表达。
- 推荐不等于安装，未使用不等于无用；不按插件数量推断成本。
- 每项建议说明任务依据，可保留、调整、补齐、暂不启用或待核实；**零修改是正常结果**。
- 尊重组织管理和宿主支持边界；状态变化重新核对，不能覆盖其他项目或后续有效修改。
- 采用复用改进记录；写入、生效、任务通过和后续效果分别显示，恢复也需验证。

数据链路：用途与约束 → 实际能力 / 有效配置 → 最小方案 → 审阅并采用 → 生效验证 → 代表性任务 → 改进记录与恢复。

| 必须核对 | 用户看到的结果 |
| --- | --- |
| 目标与前状态 | 产品版本、实际设置来源与有效值；审阅后变化则重新核对受影响项 |
| 作用域和依赖 | 会话、项目、个人/全局、组织管理分别说明；只能全局调整时先显示影响，不能绕过受管约束 |
| 加载与首个任务 | 需要重载而未完成则标记未生效；任务不通过不宣布适配完成，没有历史不虚构节省 |
| 取消、失败与恢复 | 保留已做、未做和未验证项；AI 引用或设置无效时拒绝相关建议，宿主不可用时保留可读草案 |

持久多场景档案、自动切换、自动迁移、市场和能力包属长期候选。

### 功能 2 · 健康概览与问题证据（阶段 A）

先回答当前范围内最值得关注的事项，再让用户深入事实、原因假设、建议和验证方式。**一个问题必须关联具体任务或组件及其证据。**

- 当前范围：选定项目 / 宿主 / 任务；配置、会话和消耗证据分别标注可用、部分或不可用。
- 内容顺序：需要关注（如多轮相似搜索）→ 下一步（核对任务证据）→ 可采用的改进方向 → 动作（查看建议 / 标记误判 / 忽略）。

约束：

- 范围与能力状态始终可见；**缺失证据不能按零消耗或完全健康处理**。
- 问题标题说明具体行为；事实与可能原因分开，优先级综合影响、证据、风险和任务目标。
- 查看证据直达本问题关联位置；返回保留筛选和阅读位置。
- 建议可审阅、可拒绝；标误判与忽略不会把疑点变成确定结论。

三种非正常态：

| 状态 | 行为 |
| --- | --- |
| 没有来源 | “尚未选择分析来源”，选择项目、宿主与范围后开始；**未开始 · 无健康评分** |
| 部分完成 | 已完成来源的统计和证据继续可看，其余来源显示失败原因与缺口；可查看可用结果或缩小范围 |
| 分析失败 | 解释超时也保留已有统计与证据，可缩小范围重试；**不展示无依据的完整结论** |

### 功能 3 · 配置与关系体检（阶段 A）

从规则、组件职责、引用、Hook、工具和验证入口检查配置能否协作。静态体检与实际调用证据相互补充。检查范围为当前项目拥有的 Rule / Skill / Agent / Hook / 工具配置。

| 对象 | 示例发现 | 证据状态 |
| --- | --- | --- |
| 规则 / AGENTS | 同一动作要求互斥 | 相关段落可定位 |
| Skill / Agent | 职责或输入输出衔接待核对 | 语义推断 |
| Hook / 命令 | 引用目标路径不存在 | 配置与路径 |
| 工具 / MCP | 声明可见，连接状态未知 | 待补连接证据 |

- 关系问题详情：说明涉及哪两个对象、冲突适用范围和证据；**建议不按组件数量或文档字数机械拆合**。
- 实际使用链：存在 → 加载 → 调用 → 影响决策 → 改善结果，分别呈现可见证据。
- 动作范围：查看配置证据 / 审阅建议 / 补充验证入口；**体检不会任意执行 Hook 脚本**。

约束：只检查所选来源与作用范围；路径断链可给明确事实，职责重复、触发不当等语义推断要标明待核对；未观察到调用不等于没有价值，也不自动成为可删除对象；验证覆盖检查“有没有可达验证方式”，最终产物正确性需要运行证据。

### 功能 4 · 任务诊断（阶段 A；阶段 B 增强证据聚合）

把消耗、耗时、上下文与运行异常放回任务目标和结果中解释。常规诊断在阶段 A 交付，**上下文为阶段 A 的次优先项**；同项目多 Agent 聚合在阶段 B 增强。

| 诊断维度 | 呈现内容 |
| --- | --- |
| 消耗 | 输入、输出、缓存、重试；Token / Credit / 费用分别显示 |
| 耗时 | 模型、工具生命周期、命令、Hook、人工等待、子任务与未归因区间 |
| 上下文 | 增长、重复材料、压缩与恢复的可见证据 |
| 异常 | 失败重试、无新信息循环、反复纠偏与产物贡献 |

- 时间解释：总历时与累计工作量分开。没有事件的空白区间标未归因；**并行子任务不能简单相加成用户等待时间**。
- 上下文与缓存：真实命中依赖 usage 等记录。没有压缩内容证据时，不声称具体信息已经丢失。
- 关联证据：查看原始调用、配置版本、产物与验证结果；同项目的多 Agent 记录按来源保留。

约束：任务先保持来源身份，仅凭时间相近或同项目不能合并为一次相同任务；区分等待、执行与并发工作；高消耗并不直接等于浪费，缓存间隔不能直接推算损失金额；异常分析保留必要探索与成功贡献等反例。

| 观察到的情况 | 允许的判断 | 保留边界 |
| --- | --- | --- |
| 多次相似搜索 | 候选重复探索，建议核对停止条件 | 结果可能为任务提供了必要信息 |
| 重试后仍无新信息 | 展示错误链与可能无进展区间 | 重试次数本身不能证明无效 |
| 压缩后反复纠偏 | 呈现压缩事件与后续行为 | 相关时间顺序不等于因果 |
| 某宿主 usage 不可见 | 标注缺失与来源能力 | 不估成零，也不补造成本 |

### 功能 5 · 建议、实施与改进复验（阶段 A）

近期先完成**一个真实问题 → 一项规则或 Skill 改进**的小闭环。改进记录连接当前修改与后续观察；跨窗口稳定问题身份和自动复发追踪保留为后续候选。

四段结构：变更意图 → 当前验证 → 后续效果（待观察）→ 动作（审阅具体变更 / 查看验证记录 / 查看恢复方式）。
关键判断：**当前检查通过不等于已证明节省。**

约束：实施前保留目标、前状态、授权范围、风险和恢复方式；修改必须具体到对象与内容，前状态不可解析或文件已变化时重新审阅差异；效果单独记录，结果不成立时缩小或撤回建议。

| 可见状态 | 含义与下一步 |
| --- | --- |
| 建议 / 待实施 | 尚未执行；用户可审阅、采用或忽略 |
| 实施中 / 待验证 | 保留修改与执行过程，随后执行匹配的验证 |
| 验证通过 / 验证失败 | 只裁决当前修改；失败保留原因和恢复入口 |
| 已撤销 / 已忽略 | 保留历史与原因，不自动重开为长期问题 |
| 后续效果：待观察 / 支持 / 无变化 / 冲突 / 不可比 | 效果与实施状态独立；比较条件不足时继续观察 |

### 功能 6 · 比较实验与运行方案建议（有限筛选 · B 比较 · C 效果优选）

把观察、实验和推荐分开，让用户知道结论适用于哪些条件。

| 建议层次 | 依据与结论 | 下一步 |
| --- | --- | --- |
| 能力适配 · 有限筛选 | 任务硬约束、官方能力、日期版本和当前可用性；只判断可能适用与缺口 | 保留当前、调整使用形态或准备试用；未知可见 |
| 效果优选 · 阶段 C | 代表性任务与可比实验；先质量，再看完整成本、耗时、失败、重试与人工干预 | 按适用条件采用，后续无效时撤回或收窄 |

**比较对象是一套运行方案**：Runtime/产品 + CLI/桌面/IDE/SDK 形态 + 模型/服务 + 能力配置。同一产品可有多种形态，**不能将“CLI”和“Codex”列为互斥选项**。模型与 Harness 同时改变时只比较整体方案，不武断归因。

示例（“我只需要程序化执行”）：

| 方案方向 | 适配理由 | 仍需核对 |
| --- | --- | --- |
| 沿用当前产品，调整接入形态 | 若已有可用 SDK 或 CLI，可能无需更换产品 | 目标版本、可调用工具、权限与首个任务 |
| 考虑可定制的轻量 Harness | 若需要重组 Agent 循环或能力组合，进入有限候选 | 缺失能力、维护负担、模型适配及试用结果 |
| 信息不足，先保留当前 | 没有证据证明必须迁移 | 补充硬约束或做有界试用；暂不排名 |

Codex、Qoder、Pi 与 DeepSeek Harness 可进入候选核对，具体取舍依上述条件；**本轮未进行跨 Runtime 实测**。用户保持当前环境或忽略建议后不反复劝迁移；推荐不自动授权安装、付费配置或迁移。

受控实验必须冻结的条件：任务与起点（目标、输入、代码基线、验收标准）、比较与执行（待比较变量、各自隔离范围）、成本与终止（预算、终止条件、全部尝试保留）。
结论顺序：先核对客观验收 → 再呈现人工或 AI 质量判断 → 最后比较完整成本和有效耗时。失败、重试与评委分歧一并呈现，不只保留成功样本；效果样本不足时显示“目前无法判断实测优势”。

| 模式 | 能回答 | 不能直接声称 |
| --- | --- | --- |
| 历史会话观察 | 两次工作各自发生了什么 | 同项目即同任务，或某 Agent 更好 |
| 实时并排观察 | 两个 Agent 当前的过程和结果 | 共享目录里的运行已满足公平隔离 |
| 受控实验 | 固定任务与约束下哪种方案更适合 | 一次实验可以生成全局排行榜 |

### 功能 7 · Memory 阅读与分析（阶段 B；现有阅读与分析入口保留）

延续已有目录、阅读和显式分析能力，增强来源、冲突与冗余的解释。分析围绕**用户选定快照**进行，建议与原生内容保持边界。

- 来源与范围：个人 / 通用 / 项目；保留所属宿主、来源路径和快照时间。
- 当前阅读内容：切换导航不会改变正在分析的固定快照。
- 分析前可审阅：选中的内容、发送去向、执行宿主与本次分析范围保持可见。
- 分析结果：围绕冲突、过期、冗余与引用提出有据建议；**原生记忆保持只读**。

约束：多来源可读取不等于可自动同步；没有观察到使用不代表无用；本地界面不代表推理全在本地，不静默扫描全机记忆；不自动写入、合并或同步原生 Memory，结果引用失效时撤回相应结论。

### 功能 8 · 可信分析、预算与导出（阶段 A）

让用户知道读取什么、交给谁分析、花费多少以及结果可以信到什么程度。**单来源失败不吞掉其他可用结果。**

1. 环境与数据范围：所选宿主、项目、时间和内容；按能力显示可用 / 部分可用 / 不可用。
2. 分析去向与预算：展示当前执行宿主、待发送内容、费用与取消条件；额度值由用户或产品配置确定。
3. 运行中可以取消：保留已完成的统计和证据；取消与部分完成不显示为完整分析结束。
4. 当前报告导出：选择内容范围，预览脱敏结果后导出；**原始私密证据不默认全量附带**。

约束：范围只允许保持、缩小或由用户明确扩大；分析自身也要计量（先统计后解释、缩小范围与增量分析用于控制负担）；超时保留统计、无效引用撤回结论、外部 Agent 不可用时保留只读结果；导出是当前报告动作，发送与共享由用户决定。

### 功能 9 · 双形态与专业工作区（阶段 A）

两种形态共享诊断口径，交互适配宿主能力。已有专业入口继续可达，普通用户可以先完成健康检测，再按需深入调试、产物与实验。

| 能力 | 宿主插件 / 内置入口 | Studio / 独立应用 |
| --- | --- | --- |
| 范围 | 当前宿主与项目的可用子集 | 用户选择的支持宿主、项目与时间 |
| 静态 / 动态诊断 | 沿用宿主允许的对话、报告或原生视图 | 统一结论与深度证据工作区 |
| 实施与复验 | 由当前宿主原生能力承接 | 明确委派的宿主、对象和范围 |
| 比较与建议 | 跨产品推荐不作为默认目标 | 历史观察、受控实验与有条件场景建议 |
| 记忆与报告 | 当前授权来源；同样保持只读与脱敏边界 | 多来源隔离分析与交互报告 |
| 专业能力 | 沿用原生宿主功能 | 保留 Debugger / Artifacts / Harness as Code |

原则：同输入保持同诊断含义 → 按形态呈现结果 → 在原生能力与授权范围内承接动作。
**宿主不支持的细粒度证据、实验或修复能力，不能通过桌面壳补造。**

---

## 6. Codex 插件使用形态

**插件是什么产品**：一个围绕当前项目工作的 Harness 诊断与改进助手。用户从任务对话调用 Better Harness，先读结论、按需打开证据报告，再让 Codex 继续处理已采用的建议。**完成这条过程不要求先安装 Studio。**

以下是交互脚本与报告内容方案，不是实机截图。当前仓库已有插件清单、Skill 入口、Codex 配置与会话适配及报告流程；本次没有安装或实跑插件。原生插件管理、对话和预览外观尚未取得可用实机基线，因此不绘制原生窗口或专属侧栏。

| 承载位置 | 用户看到什么 | 继续做什么 |
| --- | --- | --- |
| Codex 任务对话 | 分析范围、自然语言进展、主要发现、证据缺口和下一步 | 追问同一问题，按需打开报告，采用明确建议 |
| 本次分析的详细报告 | 范围与覆盖、配置关系或任务过程、问题证据、建议、已经发生的处理结果 | 展开证据、切换报告分组、复制绑定问题的处理请求，再回到 Codex |
| Codex 原生工作过程 | 具体变更及审阅、真实执行与检查结果 | 在已授权范围内修改和复验，把结果关联回原问题；后续效果另行判断 |

**发现、安装与首次调用**：仓库说明中的 Desktop 路径为 Settings → Plugins → Add → From Marketplace，添加 Better Harness 仓库后安装，再新建项目任务。目标版本的入口名称与加载行为仍需实机核对；**安装本身不等于授权读取全部历史或全局配置**。
调用示例：Desktop `@better-harness`，CLI `$better-harness:better-harness`。入口提示可围绕“检查当前项目配置”“分析选定任务为什么慢或贵”“继续处理报告中的一个问题”——这些是意图文案，不是新增命令的实现承诺。

**一次完整的对话过程（假设性示例）**

1. 用户发起分析：`@better-harness 分析我选定的这次 Codex 任务为什么反复重试，先给结论和证据。`
2. 插件说明范围与进展：若“这次任务”无法唯一定位，先列出可读候选，**不自动替换成最近一条**；数据不足时明确缺口，进展不显示虚构百分比。
3. 插件给结果摘要：分析对象、主要发现、证据、尚不能判断（如缺少账单信息，不能把 Token 换算为实际金额）、下一步。
4. 用户选择改进：“只处理这条建议，其他配置保持原样。”处理请求须绑定具体报告与问题；报告无法找到或已过时时先说明情况。
5. Codex 实施复验：展开修改对象、差异、验证与恢复方式；完成后分别表达“方案已生成”“已修改”“当前验证通过”“后续效果待观察”。

**详细报告结构**

| 报告区域 | 需要呈现的内容与阅读动作 |
| --- | --- |
| 范围与覆盖 | 当前项目、宿主、选定任务或时间窗口；配置、会话、usage 等分别标明可用、部分或不可用 |
| 问题与证据 | 先呈现有依据的问题，再展开引用位置、配置关系或任务过程；区分事实、原因假设和待核对内容 |
| 建议与处理请求 | 展示影响对象、拟改内容、风险、预期产物、验证和恢复方式；“复制处理请求”绑定本报告与问题 |
| 实施与后续效果 | 仅展示已经发生的修改和检查；没有新任务证据时保留“待观察”。回写不可用时在对话交付结果，原报告不假装已同步 |

报告聚焦本次问题，不复制 Studio 的全局多宿主导航。用户指定只在对话分析时可以不生成报告文件；保存到本地与对外分享分别处理。

**能力缺口与异常处理**

| 情况 | 对用户可见的处理 |
| --- | --- |
| 任务不明确 / 没有动态数据 | 先定位任务；缺少记录时继续有效的配置体检，明确无法判断的动态内容，不把缺数据当零消耗 |
| 额外数据范围 | 原始会话、用户目录、插件、全局配置与 Memory 按明确范围读取；元数据可读不等于正文获准；不承诺完全离线 |
| 取消、超时或部分失败 | 沿用宿主停止方式，保留已完成部分和缺口；不把部分结果标为完整成功 |
| 只分析 / 已采用建议 | 只分析就停在结论与建议；采用后沿用已有授权，仅在范围、权限或外部后果扩大时补充确认 |
| 报告打不开、过时或无法更新 | 保留对话结果和文件路径；过时报告重新核对，不静默换用其他报告 |
| 分享报告 | 检查所含路径、引用片段和敏感信息再分享，不默认包含全部原始记录 |
| 需要 Studio 的专业分析 | 携带报告、问题与来源信息手动继续；自动导入、深链接与状态同步尚待验证 |

MCP 与原生交互组件按后续需要评估。官方插件架构的可选 UI 说明不足以证明宿主已支持任意常驻侧栏，**本方案不据此承诺专属菜单、监控面板或原生图表卡片**。

**A 后场景选配在插件中的形态**：用户可提出“只把 Codex 当任务执行 Runtime，按这个用途核对配置”，随后审阅最小方案、采用范围、生效与代表性任务结果，并回到同一改进记录。询问选型时可先给有限能力筛选；保留当前产品与调整接入形态都是正常结果。

---

## 7. 生命周期：新项目建设与存量治理

### 7.1 新项目：先回答需要什么，再决定配置什么

```text
了解项目 → 复用已有能力 → 提出最小方案 → 采用并验证 → 完成第一项任务
```

输入围绕目标、项目类型、已知技术栈、宿主、协作方式、风险、工具与验收条件。已知信息直接复用，**只补会改变方案的关键未知**。

**最小 Harness 是一组能支撑任务的能力，不是一份固定七件套。** 已有宿主默认行为和团队规则足够时，说明复用依据即可，不为了完整度新增文件、Skill、Agent 或工具。

| 需要考虑的能力 | 最小起点 | 怎样确认够用 |
| --- | --- | --- |
| 目标与项目上下文 | 项目目标、范围、关键约束与首个任务的验收标准 | 能说明要交付什么、哪些不在本次任务内 |
| 工作方法 | 必要的操作约定；重复工作确有收益时再形成 Skill，独立责任明确时再引入 Agent | 方法与具体任务有关，职责不冲突、不重复 |
| 工具与数据 | 只接入首个任务需要且允许使用的资源 | 权限和可用性可验证；不因推荐就自动安装 MCP 或新插件 |
| 验证与交付 | 已有测试、构建或人工验收入口及结果要求 | 真实执行或明确未执行；**生成命令不等于验证通过** |
| 执行边界与恢复 | 按操作风险明确自主范围、停止条件和恢复方式 | 高影响动作有边界，常规安全动作不被重复审批阻断 |
| 工作证据 | 保留配置范围、实际任务与检查结果的必要引用 | 足以复盘，但不要求默认采集、上传全部原始历史 |
| 知识与维护 | 说明约束用途、来源、维护责任和更新条件 | 后续有人能理解并调整；不自动写回宿主原生 Memory |

第一次验收选一个有代表性的真实任务，分别确认配置存在、宿主加载、任务实际采用和交付结果。没有历史样本时衡量首个任务是否成功、设置是否费力，**不声称“比之前更省”**；首个任务通过也不证明对所有未来任务有效。初始化不自动变成模型选型或配置推荐市场。

### 7.2 存量项目：先理解，再调整

```text
盘点来源、用途与依赖 → 对照项目需要和实际使用 → 选择治理动作 → 验证与恢复准备 → 后续观察
```

| 治理动作 | 适用依据 | 执行后的结果落点 |
| --- | --- | --- |
| 保留 | 用途仍成立、约束有效，或使用证据不足以支持改变 | 说明保留原因及尚需观察的条件 |
| 补齐 | 真实任务暴露能力或验证缺口 | 针对缺口补足，核对是否能支撑原失败场景 |
| 合并 / 拆分 | 职责重叠、冲突或责任难以验证 | 检查职责与依赖是否更清楚，不以文件长短或组件数量判断成败 |
| 调整 / 替换 | 约束过时、工具不可用、环境或项目需要变化 | 核对兼容、迁移影响和代表性任务，保留必要恢复方法 |
| 退役 | 用途已消失或替代已经验证，相关依赖已厘清 | 在授权范围内退出使用，记录原因和恢复方式；**未观察到使用不等于可安全删除** |

### 7.3 怎样接住后续变化

用户可以在关键变更、迁移或交接时主动发起复查。**“持续治理”不等于后台自动监控或定时修改。** 当前先复用体检、任务诊断和改进记录；稳定的配置版本、漂移、长期复发和维护历史作为后续增强评估。
交接时留下配置用途、来源、维护责任、已知问题、验证证据与恢复方法。**归档不自动删除配置、擦除任务记录、停用服务或写入共享记忆。**

### 7.4 先验证价值，再扩展建设成本

| 需要反证的假设 | 评估时看什么 |
| --- | --- |
| 宿主默认初始化已经够用 | Better Harness 是否解决了默认工具未覆盖的具体问题；无增益时只复用默认能力 |
| 配置越全越可靠 | 新增约束是否降低首个任务成功率，或增加理解、执行与分析负担 |
| 长期没用的配置都应删除 | 是否属于低频但关键的安全、发布或恢复场景；证据不足时保留并观察 |
| 生命周期治理需要做成完整项目管理平台 | 是否仍围绕 Harness 的选择、使用与改进；不把 IDE、部署、团队管理和市场能力顺带纳入 |

---

## 8. 分阶段规划

先把一次体检做成可验证的改进；其后试点单宿主选配，并按证据成熟度推进有限筛选、可信比较和效果优选。

**优先级**：A 的主链路为 P0；上下文为 A/P1；B 的增强能力为 P1；A 后选配与有限筛选为 P1；C 的效果优选为 P2。

| 阶段 | 交付重点 | 进入 / 准出依据 |
| --- | --- | --- |
| **A · 解决一个真实问题** | 范围、配置体检、消耗、耗时、异常、结论证据、改进复验、双形态、可信分析；上下文也是 A，优先级次于主链路 | 先走通一次贵 / 慢任务到规则或 Skill 改进；用消耗、耗时、配置三类真实样本验证，包含正常反例和缺失证据例 |
| **A 后试点 · 单宿主场景选配** | 一个支持宿主、有限用途；最小方案、可审阅采用、生效与任务验证、恢复 | A 闭环稳定；逐项确认配置支持与作用域，对照原生流程检验价值，包含零修改与失败恢复 |
| **有限筛选 · 无历史的运行方案建议** | 有限有来源目录，分别筛产品、形态、模型与配置；只给适配和待试用条件 | 单宿主价值成立、目录可维护；可在完整 B 之前验证，不依赖全量实验平台，也不承诺日期 |
| **B · 可信的专业比较** | 同项目多 Agent 归集、历史观察、受控质量比较、Memory 分析增强 | A 用户能完成诊断与复检，并出现具体比较需求；数据能支撑冻结任务和验收标准的隔离实验 |
| **C · 基于实测的效果优选** | 基于可比任务建议模型档位、宿主或配置 | 质量门槛先成立；后续任务验证建议，不成立则撤回或收窄适用范围 |
| **后续进阶 · 主动发现与有界改进** | 见下一节 | 已纳入规划，**明确不属于首版**；具体版本及与 B/C 的相对顺序待定 |

### 8.1 后续进阶：主动发现与有界改进

让 Better Harness 在项目工作过程中发现值得关注的问题，再适时建议或处理已授权的有限动作。项目生命周期回答**何时需要哪类能力**，插件与 Studio 回答**在哪里使用**，主动机制补充**何时介入**。

```text
范围启用 → 捕获相关变化 → 轻量检查 → 必要的有限分析
        → 合并问题 → 适时建议或授权内处理 → 复验与后续观察
```

| 能力层次 | 主动做什么 | 用户如何掌握边界 |
| --- | --- | --- |
| 主动发现 | 在配置变化、会话开始、异常模式或工作边界做增量检查 | 先限定项目与数据范围；没有新问题保持安静，深度分析受预算限制 |
| 适时提醒 | 合并有价值的新发现，呈现证据、影响和下一步 | 可处理、稍后、忽略、标误判，并按规则或项目暂停 |
| 授权内自动处理 | 执行事先允许的有限动作，验证并记录真实结果 | 核对前状态、不覆盖进行中的编辑，有恢复方式；未授权或无法验证时停在建议 |

**触发时机**

| 时机 | 检查重点 | 介入方式 |
| --- | --- | --- |
| 首次接入 / 会话开始 | 配置可读、必要工具与验证入口可用 | 轻量核对；无新问题不重复通知 |
| Harness 配置变化 | 引用失效、冲突、职责与工具关系变化 | 只检查受影响部分，合并连续编辑 |
| 执行出现异常模式 | 重复失败、重复操作或工具持续不可用 | 结合输入与结果判断；只有继续执行明显无益时才介入 |
| 一段工作结束 | 本轮新增问题、验证缺口与已处理建议 | 汇总一次；回合结束不等于整个任务完成，不每轮重跑全量诊断 |
| 宿主升级、迁移 / 关键交付 | 兼容变化、过时约束与恢复准备 | 针对变化复查；维护时段与会话外运行另定载体和预算 |

**避免主动变成打扰**

- **检测事件不等于提醒事件。** 同一问题在范围、证据和影响未变化时不反复提醒，仅设置冷却时间仍不够。
- 普通建议放在工作边界汇总；提醒说明问题、依据、价值和具体下一步，不逐条打断。
- 遵守用户的忽略、稍后和暂停选择，不换一种说法重复劝说。
- 普通优化检查失败不阻断工作；只有用户事先启用的明确强约束才可以阻断对应操作。
- 重复操作、文件长度、组件数量或没有修改测试文件都只是线索，不能直接判定浪费或质量问题。
- 自动处理受动作、次数和预算限制；失败即停止，不因自己的改动无限触发，不自行降低检查标准或关闭约束。

**自动处理的范围与降级**：自动发现、深度分析与自动修改**分别授权**。有限自动处理必须同时具备充分证据、明确范围、已有授权、可验证结果和恢复方法。语义不明确、超预算、检查失败或宿主不可用时停在可审阅建议。优先评估 Better Harness 自己维护的派生索引等有限动作；规则语义调整、Skill 合并或退役、全局权限、模型选择、原生 Memory 与跨宿主迁移继续遵守原范围和排除项，**不能由启用检测自动获得修改权**。

**插件与 Studio 如何承接**：插件依托宿主可用事件和会话边界，在当前上下文提供建议与真实处理结果；Studio 可集中查看项目问题与治理历史。Hook 是触发来源之一，**不等于全天后台服务**。Codex 异步 Hook 完成后不会自行开启新一轮对话，会话结束会取消未完成后台 Hook；会话外持续运行需要单独设计和验证。

**准入观察项**：有效发现、经验证的改进、误报与重复提醒、用户暂停或关闭、检测成本与耗时、处理失败和恢复结果。**运行次数和通知打开量不能单独证明价值。**

### 8.2 生命周期扩展的验证顺序

| 验证范围 | 优先验证的结果 |
| --- | --- |
| 一个宿主、一类项目的新建场景 | 最小配置建议 → 用户采用 → 首个代表性任务通过；记录耗费与失败反例 |
| 同一范围内的存量治理 | 在已有体检与改进上检验来源、依赖与保留 / 调整依据，确保可验证、可恢复 |
| 更长时间跨度的治理增强 | 有持续使用证据后再评估稳定历史、漂移、复发、迁移和交接的投入 |

### 8.3 长期候选（尚未纳入近期研发容量）

| 方向 | 纳入前需要的依据 |
| --- | --- |
| 持久问题、配置档案、自动切换、复发与组件演进 | 近期改进记录与稳定匹配先有效 |
| 通用工具 / Skill 目录、市场与能力包 | 场景识别、质量、权限和兼容评估 |
| 跨 Agent 共享记忆 | 共享与隔离边界、冲突、原生兼容和用户授权 |
| 团队与组织洞察 | 明确团队用户、数据权限和运营价值 |
| 会话外定时运行与能力包 | 主动发现与有界改进已纳入后续进阶；持久调度、运行载体、预算和能力包仍需独立评估 |

---

## 9. 关键规则与异常

| 约束 | 可观察行为 |
| --- | --- |
| 安装、可见与调用混淆 | 分别取证，推荐目录不能计入已安装或运行成本；未调用不自动判定可删除 |
| 作用域、加载与恢复 | 核对有效配置优先级、受管限制及依赖；写入未生效不标成功，恢复前保护后续有效变更 |
| 无历史或候选过时 | 可给有据能力适配，效果保持未知；过时来源标待核实，不自动试用或迁移 |
| 证据不足 | 显示缺口与可补数据；模型自报置信度不显示成准确概率 |
| AI 超时或单来源失败 | 保留可用统计与证据；支持缩小范围或主动重试 |
| 幻觉或无效引用 | 撤回无效结论，不执行相关建议；原始统计保留 |
| 外部执行能力不可用 | 可以继续只读分析，说明不能运行的实验或修复 |
| 状态漂移 | 修改目标当前状态已变化时，重新审阅具体差异 |
| 费用与外部副作用 | 展示对象、范围、预算和停止条件；复用已有具体授权，范围扩大再决定 |
| 安装与分发 | 以各目标平台真实安装、启动、升级、退出证据验收；**不从 macOS 检查推断其他系统** |

产品发布前仍需确定：首批宿主能力名单、主用户、桌面分发规格、AI 分析去向与付费承担方式，以及近期实施承接形式。当前规划保留这些边界，界面不替它们填入默认承诺。

---

## 10. 成功标准与验收

先建立真实使用基线。首个可行动诊断、误判与证据不足分布、验证完成的改进、质量合格任务的成本和耗时、分析自身负担、后续回访与复用，共同说明产品价值。

**主链路验收**

- 用户可以从选定项目和任务得到一条有证据的诊断，并说清下一步。
- 高消耗但合理、耗时长但主要等待审批、组件未使用但场景不适用等反例，不被强判为浪费。
- 缺失 usage、调用或压缩证据时，报告显示未知与补数方向。
- 一条问题可以直达支撑证据；返回时保留原筛选、对象和阅读位置。
- 建议的对象、改动、授权范围、风险、验证与恢复方式均可审阅。
- 实施记录与实际修改一致；当前检查通过和后续效果分别展示。
- 所有受控实验保留冻结条件、隔离、失败和重试，先验质量再谈效率。
- Memory 分析只使用选定快照并保留引用，原生内容不被自动写回。
- 取消、超时、无效引用和宿主不可用时，保留可用结果并明确状态。
- 同一输入在两种形态中保持诊断含义一致，能力缺口和分析自身费用可见。

**A 后场景选配与有限筛选的验收**

- 无历史但用途明确，可以形成最小方案和首个验证任务；默认足够可零修改，不虚构节省。
- 推荐但未安装的插件不进入待停用清单；未调用能力仍核对用途、依赖和低频需求。
- 作用域、前状态、版本、依赖和恢复可审阅；项目授权不静默影响其他项目，组织管理限制有效。
- 真实修改、加载生效与代表性任务通过分别记录；取消、状态变化、部分失败和恢复保留真实结果。
- 只需程序化调用时先核对当前产品的接入形态；能力适配与效果优选分开，**允许没有赢家**。
- 选择保持当前或忽略后不反复劝迁移；新增指标看准备负担、首个任务、恢复及后续返工，**不看停用数量**。

**插件体验的验收落点**：在可用且授权的 Codex 环境中，用户能只通过插件完成一次配置或指定任务诊断，读到结论与证据，采用一条建议并回看真实修改和检查结果。动态数据缺失、取消、打不开报告或回写不可用时仍有明确的继续路径。

**后续主动能力的验收边界**（进阶准入条件，不加入首版验收）：主动检测不默认提醒或修改；无新证据不重复打扰，忽略与暂停有效，检测失败不阻断普通工作；已授权处理有真实修改、复验和恢复结果，超限停止且不会自我循环。

**场景选配与生命周期的分层验收**：新项目以首个任务成功、设置与分析负担、配置是否实际采用为依据；存量治理以修改依据、依赖核对、代表性任务验证和恢复能力为依据。首个任务成功率与已有任务的成本改善分开衡量，**不把“生成很多配置”当作价值**。

具体样本量、准确性门槛与商业增长目标在建立基线后决定。全量分析时长、访问量或迁移率不能单独证明改进有效。

---

## 11. 现状与来源

**Studio 现状基线**：本地构建实测 · 1440 × 900 · 浅色 · 隔离空项目。只验证共用工作台壳；没有运行真实模型，也没有以空态证明诊断能力。

**变更记录**：2026-09-12 已按用户确认整合场景选配与有限运行方案筛选；原一期与 B/C 保留，主动能力继续后置。新能力为规划，未实施个人配置或 Runtime 迁移。

**业务依据**（原稿相对路径，位于规划文档目录）

- `spec/prd/better-harness-整体框架-20260911.md`（整体框架 v1.3）
- `spec/requirements/better-harness-需求分析文档-20260911.md`（需求分析 v1.2）
- `better-harness-discussion-synthesis-20260911.md`（讨论与事实核对）
- `studio-baseline-20260911.md`（工作台基线）

**本轮内容底稿**

- `better-harness-场景选配与整体规划-方案-20260912.md`
- `spec/requirements/better-harness-场景选配-需求变更评估-20260912.md`
- `better-harness-Codex插件形态-方案-20260911.md`
- `spec/requirements/better-harness-项目生命周期-需求变更评估-20260911.md`

**能力核对（2026-09-12）**：[Codex 配置参考](https://learn.chatgpt.com/docs/config-file/config-reference) ·
[Codex SDK](https://learn.chatgpt.com/docs/codex-sdk) ·
[Qoder Agent SDK](https://docs.qoder.com/cli/sdk/overview) ·
[Pi](https://pi.dev/) ·
[DeepSeek Harness](https://www.deepseek.com/harness/)。
官方能力只支持候选方向，目标版本和实际效果仍需验证。

**主动机制依据**：[Codex 生命周期与后台 Hook](https://learn.chatgpt.com/docs/hooks#run-hooks-in-the-background) ·
[冻结仓库的 Stop 检查入口](https://github.com/QoderAI/better-harness/blob/e1538c15a98856b3349f365d951f4fa0bcc33f24/hooks/review-trigger/README.md)。
这些基础不表示完整主动治理已实现。

**插件依据**：[仓库安装与调用说明](https://github.com/QoderAI/better-harness/blob/e1538c15a98856b3349f365d951f4fa0bcc33f24/README.md#codex) ·
[宿主适配说明](https://github.com/QoderAI/better-harness/blob/e1538c15a98856b3349f365d951f4fa0bcc33f24/docs/adapters/README.md) ·
[问题绑定修复流程](https://github.com/QoderAI/better-harness/blob/e1538c15a98856b3349f365d951f4fa0bcc33f24/skills/better-harness/references/finding-bound-fix.md) ·
[官方插件架构](https://developers.openai.com/plugins/concepts/plugins) ·
[官方 UI 适用范围](https://developers.openai.com/plugins/deploy/troubleshooting#how-to-triage-issues)。
以上来源不替代目标版本的实机验证。

**原始讨论**：[会议逐字稿](https://alidocs.dingtalk.com/i/nodes/vy20BglGWOxjGpq0CvQdp7EvVA7depqY) ·
[同场会议 AI 纪要](https://alidocs.dingtalk.com/i/nodes/MNDoBb60VLYDGNPytaw45OPnJlemrZQ3)（两份文档来自同一会议）。
仓库核对基于 [e1538c1 源码快照](https://github.com/QoderAI/better-harness/tree/e1538c15a98856b3349f365d951f4fa0bcc33f24)。
